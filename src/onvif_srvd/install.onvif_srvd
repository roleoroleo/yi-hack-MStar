#!/bin/bash

SCRIPT_DIR=$(cd `dirname $0` && pwd)
cd $SCRIPT_DIR

# Compress the file
#upx ./_install/sbin/pure-ftpd

mkdir -p ../../build/home/yi-hack/sbin/

rsync -a ./_install/bin/onvif_srvd ../../build/home/yi-hack/bin/
